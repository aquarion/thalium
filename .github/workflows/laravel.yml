name: "[Tests] Acceptance Testing"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    uses: istic/shared-workflows/.github/workflows/laravel-tests.yml@main
    with:
      php_version: "8.3"
      node_version: "22"
      test_command: "vendor/bin/phpunit"
    secrets: inherit # pragma: allowlist secret
